lifecycle:
  startup:
    port: 5000

commands:
  send:
    arguments:
      data:
        type: string
      channel:
        type: string
    http:
      method: post
      endpoint: /send

  run:
    command: ["node", "slackReceive.js"]
